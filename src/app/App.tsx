import React, { useState } from 'react'
import { AppRouter } from './providers/router'

const App = () => {

	return (
		<div className={`app default`}>
			<AppRouter />
		</div>
	)
}

export default App
